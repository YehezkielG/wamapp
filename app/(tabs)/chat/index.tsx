import { View, Text } from 'react-native';

export default function Chat() {
  return (
    <View className="flex-1 items-center justify-center bg-slate-50">
      <Text className="text-xl font-semibold text-slate-800">Chat</Text>
    </View>
  );
}